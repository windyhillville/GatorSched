import { Screen } from '@/ui/layout';
import { Text } from 'react-native';

export default function Availability() {
  return (
    <Screen centered>
      <Text>This is the Availability Screen!</Text>
    </Screen>
  );
}
